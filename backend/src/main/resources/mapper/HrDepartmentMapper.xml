<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hr.mapper.HrDepartmentMapper">

    <sql id="Base_Column_List">
        id, dept_name, parent_id, dept_code, leader_id, sort, status, create_time, update_time, is_deleted
    </sql>

    <sql id="Where_Clause">
        WHERE is_deleted = 0
        <if test="q.deptName != null and q.deptName != ''">
            AND dept_name LIKE CONCAT('%', #{q.deptName}, '%')
        </if>
        <if test="q.status != null">
            AND status = #{q.status}
        </if>
    </sql>

    <select id="selectPage" resultType="HrDepartment">
        SELECT <include refid="Base_Column_List"/>
        FROM hr_department
        <include refid="Where_Clause"/>
        ORDER BY sort ASC, id ASC
        LIMIT #{offset}, #{q.size}
    </select>

    <select id="selectCount" resultType="long">
        SELECT COUNT(*) FROM hr_department
        <include refid="Where_Clause"/>
    </select>

    <select id="selectById" resultType="HrDepartment">
        SELECT <include refid="Base_Column_List"/>
        FROM hr_department WHERE id = #{id} AND is_deleted = 0
    </select>

    <select id="selectAll" resultType="HrDepartment">
        SELECT <include refid="Base_Column_List"/>
        FROM hr_department WHERE is_deleted = 0 ORDER BY sort ASC, id ASC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO hr_department (dept_name, parent_id, dept_code, leader_id, sort, status)
        VALUES (#{deptName}, #{parentId}, #{deptCode}, #{leaderId}, #{sort}, #{status})
    </insert>

    <update id="update">
        UPDATE hr_department
        SET dept_name = #{deptName}, parent_id = #{parentId}, dept_code = #{deptCode},
        leader_id = #{leaderId}, sort = #{sort}, status = #{status}
        WHERE id = #{id} AND is_deleted = 0
    </update>

    <update id="logicDelete">
        UPDATE hr_department SET is_deleted = 1 WHERE id = #{id} AND is_deleted = 0
    </update>
</mapper>
