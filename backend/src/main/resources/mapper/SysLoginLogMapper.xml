<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hr.mapper.SysLoginLogMapper">

    <insert id="insert">
        INSERT INTO sys_login_log (username, ip, status, msg)
        VALUES (#{username}, #{ip}, #{status}, #{msg})
    </insert>

</mapper>
