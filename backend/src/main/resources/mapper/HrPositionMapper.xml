<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hr.mapper.HrPositionMapper">
    <sql id="Base_Column_List">
        id, post_name, post_code, dept_id, headcount, remark, status, create_time, update_time, is_deleted
    </sql>
    <sql id="Where_Clause">
        WHERE is_deleted = 0
        <if test="q.postName != null and q.postName != ''">
            AND post_name LIKE CONCAT('%', #{q.postName}, '%')
        </if>
        <if test="q.deptId != null">
            AND dept_id = #{q.deptId}
        </if>
        <if test="q.status != null">
            AND status = #{q.status}
        </if>
    </sql>
    <select id="selectPage" resultType="HrPosition">
        SELECT <include refid="Base_Column_List"/> FROM hr_position
        <include refid="Where_Clause"/>
        ORDER BY id ASC LIMIT #{offset}, #{q.size}
    </select>
    <select id="selectCount" resultType="long">
        SELECT COUNT(*) FROM hr_position <include refid="Where_Clause"/>
    </select>
    <select id="selectById" resultType="HrPosition">
        SELECT <include refid="Base_Column_List"/> FROM hr_position WHERE id = #{id} AND is_deleted = 0
    </select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO hr_position (post_name, post_code, dept_id, headcount, remark, status)
        VALUES (#{postName}, #{postCode}, #{deptId}, #{headcount}, #{remark}, #{status})
    </insert>
    <update id="update">
        UPDATE hr_position
        SET post_name = #{postName}, post_code = #{postCode}, dept_id = #{deptId},
        headcount = #{headcount}, remark = #{remark}, status = #{status}
        WHERE id = #{id} AND is_deleted = 0
    </update>
    <update id="logicDelete">
        UPDATE hr_position SET is_deleted = 1 WHERE id = #{id} AND is_deleted = 0
    </update>
</mapper>
